<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- For file uploads (e.g. KYC document/photo upload) inside the WebView -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- For fingerprint/biometric unlock -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- For push notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.TrueData"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.TrueData.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".WelcomeActivity"
            android:exported="false"
            android:theme="@style/Theme.TrueData" />

        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.TrueData" />

        <activity
            android:name=".MeActivity"
            android:exported="false"
            android:theme="@style/Theme.TrueData" />

        <activity
            android:name=".LockActivity"
            android:exported="false"
            android:theme="@style/Theme.TrueData" />

    </application>

</manifest>
